contracts.root=../contracts
checks.db.path=../checks.db
# Optional JDBC override for PostgreSQL (takes precedence over checks.db.path):
# checks.db.url=jdbc:postgresql://localhost:5432/contracts
# checks.db.username=contracts_user
# checks.db.password=change-me
